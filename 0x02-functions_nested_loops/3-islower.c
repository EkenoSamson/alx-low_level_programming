#include "main.h"

/**
  * _islower - a function that checks for lowercase character
  * Return: 1 when character is lowercase otherwise 0
  */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
